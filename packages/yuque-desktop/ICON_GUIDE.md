# 🎨 图标准备指南

## 方案选择

### 方案 1: 使用我提供的 SVG 模板（最快）✅

我已经创建了一个简单的图标设计：
- 绿色渐变背景（语雀品牌色）
- 白色云朵（代表云同步）
- 文档图标（代表本地文档）
- 蓝色下载箭头（代表同步）

**使用步骤：**
```bash
cd packages/yuque-desktop

# 1. 安装 ImageMagick（如果还没安装）
brew install imagemagick

# 2. 生成所有格式的图标
./generate-icons.sh
```

### 方案 2: 使用在线工具自定义

**推荐工具：**

1. **Figma** (免费，专业)
   - https://www.figma.com
   - 使用模板设计
   - 导出 1024x1024 PNG

2. **Canva** (免费，简单)
   - https://www.canva.com
   - 搜索 "app icon"
   - 自定义颜色和元素

3. **IconKitchen** (专门做应用图标)
   - https://icon.kitchen
   - 上传图片或选择图标
   - 自动生成所有格式

**设计完成后：**
```bash
# 将 PNG 文件保存为 build/icon-source.png (1024x1024)
# 然后运行
./generate-icons.sh
```

### 方案 3: 使用 AI 生成

**ChatGPT / Midjourney / DALL-E：**

提示词示例：
```
Create a modern, minimalist app icon for a document sync application.
Features: cloud icon, document, sync arrows, green gradient background.
Style: macOS Big Sur, rounded square, clean design.
Size: 1024x1024, PNG format.
```

## 设计建议

### 颜色方案

**选项 1: 语雀绿（推荐）**
- 主色：`#52C41A` (语雀绿)
- 辅色：`#1890FF` (蓝色)
- 背景：渐变 `#52C41A` → `#389E0D`

**选项 2: 专业蓝**
- 主色：`#1890FF`
- 辅色：`#52C41A`
- 背景：渐变 `#1890FF` → `#096DD9`

**选项 3: 中性灰**
- 主色：`#595959`
- 辅色：`#52C41A`
- 背景：渐变 `#8C8C8C` → `#595959`

### 图标元素建议

**核心元素（选 2-3 个）：**
- ☁️ 云朵（云同步）
- 📄 文档（文档管理）
- ⬇️ 箭头（下载/同步）
- 📁 文件夹（本地存储）
- 🔄 循环箭头（双向同步）
- 📚 书本（知识库）

### 设计规范

- **尺寸**: 1024x1024 像素
- **格式**: PNG（透明背景）或 SVG
- **圆角**: macOS 风格约 22% (226px)
- **边距**: 留 10% 的安全边距
- **细节**: 在 16x16 时仍清晰可辨

## 快速开始

### 如果你想用我的设计：

```bash
cd packages/yuque-desktop
brew install imagemagick  # 如果还没安装
./generate-icons.sh
```

### 如果你有自己的设计：

```bash
# 1. 将你的 1024x1024 PNG 保存为
#    packages/yuque-desktop/build/icon-source.png

# 2. 运行生成脚本
cd packages/yuque-desktop
./generate-icons.sh

# 3. 检查生成的文件
ls -lh build/icon.*
ls -lh build/icons/
```

## 验证图标

### macOS
```bash
# 查看 .icns 文件
qlmanage -p build/icon.icns

# 或在 Finder 中预览
open build/
```

### Windows
```bash
# 查看 .ico 文件（需要 Windows 或 Wine）
open build/icon.ico
```

## 常见问题

**Q: 没有 ImageMagick 怎么办？**
```bash
brew install imagemagick
```

**Q: 我不会设计怎么办？**
使用我提供的 SVG 模板，或使用在线工具：
- https://icon.kitchen (最简单)
- https://www.canva.com (模板丰富)

**Q: 图标太复杂，小尺寸看不清？**
简化设计，减少细节，增加对比度。

**Q: 想要专业设计？**
可以在 Fiverr 或猪八戒网找设计师，费用约 $5-50。

## 下一步

图标准备好后：
```bash
npm run build:mac
```
